#include <stdio.h>
#include <iostream>
#include "chinese.h"
使用 命名空间 标准;

整型 主(){
	输出缓存 << "世界，你好" << 换行;
	布尔 标志 ；
	标志 = 真 ；
	如果 （ 标志 ）{
		输出缓存 << "标志为真" << 换行;
	}
	否则{
		输出缓存 << "标志为假" << 换行;
	}
	系统(等待按键按下)；
	返回 0; 
} 
